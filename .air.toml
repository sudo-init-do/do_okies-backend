# Air config for live reload
root = "/app/apps/api"     # ✅ tell Air where your source lives
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ./"
bin = "./tmp/main"
full_bin = "./tmp/main"
include_ext = ["go"]
exclude_dir = ["vendor", "tmp"]

[log]
time = true
